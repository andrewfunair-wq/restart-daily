name: Daily knowledge library check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check built-in knowledge library
        run: test -s knowledge/fallback.js
